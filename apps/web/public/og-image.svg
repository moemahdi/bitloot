<svg width="1200" height="630" viewBox="0 0 1200 630" fill="none" xmlns="http://www.w3.org/2000/svg">
  <!-- Background -->
  <rect width="1200" height="630" fill="#0A0E1A"/>
  
  <!-- Gradient overlay -->
  <defs>
    <linearGradient id="bgGradient" x1="0" y1="0" x2="1200" y2="630">
      <stop offset="0%" stop-color="#00D9FF" stop-opacity="0.1"/>
      <stop offset="50%" stop-color="#9D4EDD" stop-opacity="0.05"/>
      <stop offset="100%" stop-color="#FF006E" stop-opacity="0.1"/>
    </linearGradient>
    <linearGradient id="textGradient" x1="0" y1="0" x2="1" y2="0">
      <stop offset="0%" stop-color="#00D9FF"/>
      <stop offset="100%" stop-color="#9D4EDD"/>
    </linearGradient>
  </defs>
  
  <rect width="1200" height="630" fill="url(#bgGradient)"/>
  
  <!-- Grid pattern -->
  <pattern id="grid" width="64" height="64" patternUnits="userSpaceOnUse">
    <path d="M 64 0 L 0 0 0 64" fill="none" stroke="#00D9FF" stroke-width="0.5" stroke-opacity="0.1"/>
  </pattern>
  <rect width="1200" height="630" fill="url(#grid)"/>
  
  <!-- Glow orb left -->
  <circle cx="100" cy="100" r="200" fill="#00D9FF" fill-opacity="0.15" filter="blur(60px)"/>
  
  <!-- Glow orb right -->
  <circle cx="1100" cy="530" r="180" fill="#9D4EDD" fill-opacity="0.12" filter="blur(50px)"/>
  
  <!-- Logo/Brand -->
  <text x="600" y="250" font-family="system-ui, -apple-system, sans-serif" font-size="100" font-weight="700" text-anchor="middle" fill="url(#textGradient)">BitLoot</text>
  
  <!-- Tagline -->
  <text x="600" y="340" font-family="system-ui, -apple-system, sans-serif" font-size="36" font-weight="500" text-anchor="middle" fill="#FFFFFF">Crypto Gaming Marketplace</text>
  
  <!-- Subtitle -->
  <text x="600" y="400" font-family="system-ui, -apple-system, sans-serif" font-size="24" font-weight="400" text-anchor="middle" fill="#B4BDD0">Instant Game Keys • Pay with Bitcoin &amp; 300+ Cryptos</text>
  
  <!-- Bottom accent line -->
  <rect x="400" y="480" width="400" height="4" rx="2" fill="url(#textGradient)"/>
  
  <!-- Trust badges text -->
  <text x="600" y="540" font-family="system-ui, -apple-system, sans-serif" font-size="18" font-weight="400" text-anchor="middle" fill="#7A8599">⚡ Instant Delivery • 🔒 Secure Payments • 🎮 1000+ Games</text>
</svg>
